services:
  build:
    image: ghcr.io/${GHCR_OWNER:-mkapusnik}/duel6r-build:develop
    working_dir: /workspace
    volumes:
      - ./:/workspace
    environment:
      BUILD_TYPE: Release
      D6R_RENDERER: gl4
      D6R_WITH_LUA: "ON"
      OUTPUT_DIR: build
